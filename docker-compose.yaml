services:
  db:
    image: postgres:15.14-alpine3.21
    environment:
      POSTGRES_DB: cli
      POSTGRES_USER: cli_user
      POSTGRES_PASSWORD: cli_pass
    ports:
      - "54210:5432"
    healthcheck:
      test: [CMD-SHELL, pg_isready -U cli_user -d cli_pass]
      interval: 5s
      timeout: 3s
      retries: 20